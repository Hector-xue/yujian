name: yujian
description: "余见 Yujian · 本地优先的个人财务 Agent"
publish_to: 'none'
version: 0.8.17+28

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  ledger_core:
    path: ../../packages/ledger_core
  interpreter:
    path: ../../packages/interpreter
  query_dsl:
    path: ../../packages/query_dsl
  providers:
    path: ../../packages/providers
  persona:
    path: ../../packages/persona
  notification_templates:
    path: ../../packages/notification_templates
  sync_client:
    path: ../../packages/sync_client
  sqlite3: ^3.6.0
  sqlite3_flutter_libs: ^0.5.0
  path_provider: ^2.1.0
  path: ^1.9.0
  shared_preferences: ^2.3.0
  flutter_secure_storage: ^9.2.0
  file_picker: ^10.0.0
  image_picker: ^1.1.0
  speech_to_text: ^7.4.0
  record: ^6.1.0
  sherpa_onnx: ^1.13.8
  flutter_tts: ^4.2.0
  just_audio: ^0.10.6
  archive: ^3.6.1
  http: ^1.2.0
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/widgets/
  shaders:
    - shaders/glass.frag
