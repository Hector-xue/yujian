<?xml version="1.0" encoding="utf-8"?>
<!-- 4×2「目标」：头行 可花的 + 称号胶囊；下面最多 3 个目标（emoji · 名字 · 还差多少 + 进度条），行由代码 addView 进来。
     没目标时显示一句话。点整块 → 目标页；点头行 → 首页。RemoteViews 只认基础控件（ProgressBar 是允许的）。 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_bg"
    android:paddingLeft="18dp"
    android:paddingRight="18dp"
    android:paddingTop="12dp"
    android:paddingBottom="10dp">
    <LinearLayout
        android:id="@+id/widget_head"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">
        <TextView android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="可花的" android:textSize="12sp" android:textColor="#1B6BC7" android:textStyle="bold"/>
        <TextView android:id="@+id/widget_disposable" android:layout_width="0dp" android:layout_weight="1" android:layout_height="wrap_content" android:layout_marginLeft="6dp" android:text="¥ 0.00" android:textSize="15sp" android:textColor="#1B6BC7" android:textStyle="bold" android:maxLines="1" android:ellipsize="end"/>
        <TextView android:id="@+id/widget_title_badge" android:layout_width="wrap_content" android:layout_height="wrap_content" android:layout_marginLeft="6dp" android:background="@drawable/widget_badge_bg" android:paddingLeft="8dp" android:paddingRight="8dp" android:paddingTop="2dp" android:paddingBottom="2dp" android:text="" android:visibility="gone" android:maxLines="1" android:textSize="11sp" android:textColor="#1B6BC7" android:textStyle="bold" android:includeFontPadding="false"/>
    </LinearLayout>
    <LinearLayout
        android:id="@+id/widget_goal_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="4dp"
        android:orientation="vertical"
        android:gravity="center_vertical"/>
    <TextView android:id="@+id/widget_goals_empty" android:layout_width="match_parent" android:layout_height="wrap_content" android:visibility="gone" android:text="还没有目标 · 点这里建一个，钱才有方向" android:textSize="12sp" android:textColor="#6C7580" android:maxLines="2"/>
</LinearLayout>
