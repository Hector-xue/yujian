# 更新日志

## 0.4.0 — 未发布

- 交易编辑表单（金额/时间/账户/分类/商户/说明）、分类管理（子分类/改名/移动/删除）、账户编辑/归档/多币种
- 查询 forecast（月底预计/日均/还能花多少）与存钱算术；异常支出识别（首页 + 对话 + MCP）
- 系统分享进余见：文字进对话，图片进截图识别
- App 内麦克风语音输入（Android/iOS/macOS/Web），说完自动发出，草稿仍需确认
- Anthropic 原生 Provider；能力探测加看图；仅本地模型 / 发送前脱敏开关
- 导入：账单表格支持手工列映射；导出：Markdown 月报、SQLite 数据库文件备份与恢复（原生端，恢复会重置同步身份）
- 服务端 AI 代理（密钥放服务端，多设备共用）；语料补到 100 条
- 人格头像与主题色：内置五个人格各有 emoji 和强调色，自定义人格包可配 `emoji` / `accent`，App 强调色随人格切换

## 0.3.0 — 2026-09-15

- 可选 Yujian Server（server/，Python，Docker Compose）：同步中继 + 加密备份，不含账本规则
- 变更日志 + 多设备同步：推/拉/逐条应用，实体级 LWW，被覆盖方记审计；应用远端变更不回推
- 加密云备份：口令 PBKDF2 → AES-256-GCM，服务端只见密文；从服务器恢复
- 模型调用改用 package:http，Web 端也能接模型
- App 图标、apk 按 ABI 拆包
- 用户通知模板（App 内新建）、自定义人格包导入；自托管/人格/模板文档

## 0.2.0 — 2026-09-15

- 周期账单：到期只生成草稿进收件箱；补期上限 3；月末对齐
- 预算：分类（含子类）或总支出；退款按净额；按起始日对齐周期；提醒线；首页提示
- Android 通知自动记账：只读通知监听 + 内置模板（微信/支付宝/银行/泛用）+ 用户模板；确认/智能/静默三种模式；自动入账记录可撤销；模板试验框
- 截图/小票识别：视觉模型走同一套 JSON 归一与账本校验，置信打折进收件箱；可单独指定看图模型
- MCP Server：stdio JSON-RPC，只读 + propose 工具；Release 提供 Linux/Windows 原生 bundle
- 桌面：Linux / Windows 包
- 对话直接回答「固定账单有哪些」「预算还剩多少」

## 0.1.0 — 2026-09-15

首个版本。单设备本地账本，不需要服务器。

- 对话记账：规则解析优先（<10ms），可接任意 OpenAI 兼容模型补齐；金额与文本交叉核对；模型不可用自动退回规则
- 收件箱：一切候选先进收件箱，逐条可改、整组确认（原子），缺字段不能确认
- 账本核心：交易 + posting 轻复式，余额由流水推导；整数最小货币单位；退款不超原单；作废带原因；审计日志全链可追
- 查询：Query DSL，模型只出 JSON，引擎在账本上执行并给依据交易数；支持分组、对比期、余额
- 财务记忆：商户/关键词 → 分类/账户，收件箱里的纠正权重最高
- 人格：5 个内置人格；提示词五段分层，人格包只能改风格段
- 模型配置中心：OpenAI 兼容端点，测试连接真发请求测能力；API Key 进系统安全存储
- 数据：导出 CSV、JSON 全量备份、恢复（整库替换）、导入微信/支付宝/自家 CSV（去重、进收件箱）
- 平台：Android（签名 apk）、Web（IndexedDB 存储，用于演示）

已知限制：单币种统计不折算；无周期账单、预算、通知自动记账（0.2 计划）；iOS 未构建。
