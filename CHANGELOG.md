# 更新日志

## 0.9.18 — 2026-09-26

把各页面上看得到的汇总数字挨个对了一遍，同一笔钱在两个页面上是两个数的，都改成一个数。

- **还款计划和「可花的」对不上**：同一份账，「可花的」按发薪前要付 2239.11 扣，还款计划里却是 0。原因有两个：发薪当天到期的账单，「可花的」算、计划不算；收件箱里还没确认的周期账单（比如今天到期的月供），「可花的」扣了、计划里压根没有。现在两边算同一批账单，计划标题改成「到 X 号发薪为止要还（含当天到期的）」，过了日子还没确认的挂在今天
- **还清目标的「还欠」停在旧数**：信用卡边还边刷，欠款涨过建目标时的数以后，负债页写欠 8618.28，首页目标条和小部件还写 8118.28。现在「还欠」永远等于这个账户此刻欠多少
- **目标的钱放在余额宝，「可花的」被多扣一次**：余额宝这类投资账户本来就不算在手头余额里，锁进目标时又从「可花的」里扣了一遍。现在只扣算在手头余额里的。「钱放哪」只列能存钱的账户（信用卡、贷款不能当锁仓），并写明这个账户里原有的钱会直接算作已攒
- **净资产 ≠ 资产 − 负债**：页面写「净资产 = 资产 − 负债」，但透支成负数的钱包，资产那边没减、净资产里减了，三个数加减不起来。现在资产按账户相加（透支的照减），等式严格成立
- **记录页**：一天里有人民币和美元时，小计把两种币直接加在一起还标成美元（「支出 -80.00 USD」），现在分币种写（「支出 ¥200.00 + 20.00 USD」），只有退款的一天写「退回」。以前只列最近 500 笔、更早的看不到，现在底部能「加载更多」，被截在中间的那一天补全，小计和日历对得上
- 还清目标详情的「依据」写的是账户的内部编号，改成账户名；首页有外币账户时注明没算进余额和总资产；体检页有月供时「月支出」注明含月供

## 0.9.17 — 2026-09-26

**负债页的「每月还款」「预计还清」算错了**
- **每月还款只算了贷款**：有信用卡时，卡上这期要还的钱一分没算进去（三张卡欠 1.9 万、10 月初到期，页面却只写每月还 688.97）。现在「每月还款」= 贷款月供 + 每张卡最近一期要还的：本期没还清按还剩的（逾期了连违约金和利息），本期还清了按出账后新刷的，没设账单日的卡按全部欠款算。有信用卡时下面拆开写「贷款月供 + 信用卡最近一期」
- **「占本月收入」月初乱跳**：分母原来是这个自然月到今天的收入，工资没到时是「—」或只按零头算。改成「占月收入」，按近几个月的平均月收入算（和体检同一个口径）
- **预计还清拿总数去除**：以前是「贷款总余额 ÷ 月供总和」，两笔安逸花（一笔还要 9 个月、一笔 2 个月）被算成 4 个月。现在每笔各算各的，取最晚还清的那笔；有一笔欠着却没设每月还款时写「没设还款」。同时有信用卡时标成「贷款还清」
- 资产页、「更多」里的「每月还」同一个口径。等级、今天还能花、体检的「月供占收入」仍只算贷款月供：信用卡刷的时候已经记成支出了，再算一遍就重复扣了

## 0.9.16 — 2026-09-26

全面检查了一遍代码，修的是「会漏记、会记重、会算错」的问题，外加几样之前缺的功能。

**会漏记 / 记重的**
- **微信第二笔支付被当成重复吞掉**：微信同一个会话的通知一直复用同一个通知位，以前按通知位去重，第一笔之后同一来源的支付都被当成「同一条通知」静默丢了。现在按通知位 + 通知内容去重，只挡系统重发的同一条
- **同一笔被抓好几次**：通知、支付页识别、截图、账单导入各抓一次同一笔钱，以前互相认不出。现在自动来源进来的新草稿会和前后 10 分钟内金额、方向都一样的已记账 / 待确认草稿比一下，撞上就标「疑似重复」、不自动入账，详情里写明和哪一条撞了
- **每月定存一期存两次**：同一条「每月 X 号存」规则，到期定存存一次、发薪日分钱又存一次。现在两条路共用一个记号，谁先到谁存
- **定存那天没打开 App 就漏一期**：现在当期内哪天打开都补上（建目标之前的那期不倒扣）；零头周结补最近 4 周
- **两台设备各记一遍周期账单**：一台确认了、同步到另一台，另一台再确认时直接认领那一笔，不再重复入账

**同步**
- **同步卡死**：别的设备推来一条这台应用不了的变更（比如删了一个这台还在用的分类），以前同步就永远卡在这一条。现在这类变更跳过并留痕（同步页会提示条数、原文在审计日志），不影响后面的；本机还在用的分类不会被远端删掉
- **从数据库文件恢复后被同步盖回去**：以前恢复完从头拉取，服务器上的历史会把刚恢复的内容悄悄盖掉，恢复的内容也不会推给别的设备。现在恢复出来的内容算作这台的新改动，推上去、盖过旧的
- **同步加密（可选）**：同步页填一个同步加密口令（至少 8 位，所有设备填同一个），同步内容就端到端加密，服务器只看得到密文，看不到金额、商户、账户名。开了之后可以「用本机账本重建服务器数据」把旧的明文清掉。同步页和隐私声明原来写「服务器看不到你的账本」不对（没开加密时变更是明文），已改成按实际写
- **设备管理**：同步页能看服务器上有哪几台设备，丢了的手机可以「移出」（它再同步会被拒），随时能恢复；「压缩服务器日志」每条记录只留最新版本（同步成功后每 7 天自动一次），本机的变更日志同步后也压缩
- 服务端升到 0.5.0：新增设备 / 压缩 / 重建接口，AI 代理支持流式、共用一个连接池（以前每次请求都新建连接且不关）

**算错的**
- **发薪日仪式从来没按日子触发过**：判断写错了（拿「下一个发薪日」和今天比，永远不相等），现在发薪日当天会按往常工资给出分钱方案
- **发薪日推断被大额礼金带偏**：工资 15 号到、两个月 3 号各收了一笔更大的红包，以前推成 3 号。现在工资 / 奖金优先，本月工资还没到时不参与
- **「今天还能花」少扣了固定支出**：每周一次的固定支出，离发薪还有 25 天以前只扣一次，现在按实际次数扣；已经到期、还在收件箱没确认的周期账单也先扣上
- **房租这类周期账单把「无消费日」任务算破功了**：说好不算的，以前其实算了
- **账户归档以后，它上面的旧账改不了**（连改个分类都报「账户已归档」），改失败还在收件箱留一条草稿。现在旧账照常能改，失败也不留草稿
- 目标释放时按比例退回，偶尔多退 1 分；达成但没释放完的心愿里的钱会被算成「可花的」；生存月数在临界点来回抖时反复喊「升级」；第一个月数据很少时就解锁「半年应急金」「存下一半」这类成就——都修了
- 新版本同步来的新类型，老版本读到不再整页崩

**说一句话记账**
- 「咖啡三点五元」以前记成凌晨 3 点，「坐地铁 3 号线 4 元」以前记到本月 3 号，「2 月 31 号」以前溢出成 3 月 3 号——都修了
- 「刚才那笔改成奖金」这类改收入分类的话以前认不出
- 「转账给房东 2000」「转给爸妈 3000」记成支出（以前当成转账还缺转入账户）
- 句子里有金额时，不再被「花多了」「大额」「预算」这类问法拦下去（以前「今天花多了，打车花了 80」那 80 块不会记）
- 模型偶尔把金额按「分」回，以前会多乘 100

**账单导入 / 退款**
- 有交易单号就按单号去重，同一分钟两杯同价咖啡不再被当成一笔；商品说明里带换行的也不再错行
- 「不计收支」（零钱通、余额宝转入转出、还信用卡）按转账起草，不再当成支出；分不清收支的不再默认当支出，收件箱里选
- 退款（导入、通知、截图）记成退款冲减原来那笔支出，不再算成收入；会按商户 / 金额找原单，找不到就在收件箱里挑「退的是哪一笔」。收件箱编辑里能改收支类型了
- 导出的 CSV 防公式注入（商户名以 = + - @ 开头的，Excel 打开不会被当公式）

**截图 / 自动入账**
- 截图里是一屏账单列表（微信 / 支付宝账单页），本机能认出每一笔，逐笔进收件箱
- 「本机认不出时发文字」那一档，模型认出的结果把握打折，智能模式下不会直接入账（截图上的文字是外部来的）
- 对话里同一张图本机识别两次不会记两笔

**其他**
- **默认记账账户**：账户页里可以设（以前固定是排第一的账户）
- **预算**：点一条就能改名称、上限、范围、周期、结束日期；过了结束日不再提醒，单列在「已结束」
- 只提醒不自动生成的周期项，过期后会滚到下一期（以前停在过去，再也不提醒）
- **应用内更新**：下载完核对安装包的 SHA-256，对不上丢弃换线路；32 位手机下 32 位的包
- **账本不进手机系统备份**（Google 云备份、换机迁移都排除），换机请用「数据」页导出 / 备份；隐私声明同步写明
- 记一笔之后的指标重算快了很多（最近几个月的记录只取一次、余额一次算完），账本几千笔也不卡；审计日志里 90 天前的同步流水自动清掉

## 0.9.15 — 2026-09-25

- **修：二级页面玻璃卡片右边发灰**（关于、设置这类点进去的页面，卡片从屏幕 80% 往右整片发灰）。真原因：页面推进来时从右边 1/4 屏宽处滑入，卡片在第一帧按「还在右边」的位置去背景上取样，动画期间不重画，停下来还是那个偏右的位置。现在卡片跟着页面进出场动画逐帧重画。0.9.13 那次按「纹理补齐到 2 的幂」修的是误判。屏幕最下面那一道渐暗（背景半像素边缘半透明、模糊后晕开）也一起修了
- **反馈草稿自动保存**：写到一半返回、切走、App 被杀，再进来文字、类型、联系方式、截图都还在；发送成功才清空
- **收件箱写明每一条从哪来、什么时候进来的**：每组顶上一行「微信 通知 · 3 分钟前」「截图 Screenshot_… · 昨天 21:04」「周期账单「房租」到期」……点「从哪来的」看详情：进入收件箱的确切时间（到秒）、通知 / 支付页的原文、截图文件名和截图时间、是本机认的还是哪个模型认的、把握多少、疑似和哪一笔重复，以及不想再要这类时去哪关。收件箱按进来时间新的在前
- **我发过的反馈**：反馈页右上角能看发送成功过的每一条（编号、时间、内容、截图），长按或点「⋯」能复制编号 / 内容、从本机删掉。只存在这台手机上，最多 50 条
- **检查更新改从 GitHub 下载**：国内直连 GitHub 下载比余见自己的服务器快得多，安装包改从 GitHub Release 下（门户和 App 内的下载都是这样，门户那边已经换了）。GitHub 连不上、20 秒连不通或 30 秒没收到数据，自动换备用线路（yujian.ivyea.com）；下载中嫌慢可以点「太慢？换备用线路」。Windows / Linux 的更新提示也多了「备用线路」按钮。出网记录按实际走的线路记，隐私声明同步改了

## 0.9.14 — 2026-09-25

- **隐私声明「一句话」**改成：本地运行的单机记账——不用注册，账本不上云，没有广告和统计；大模型和云端语音可选、自己配置，发送前先打码；除此之外只有检查更新和你主动发的反馈会联网，每一次都记在「出网记录」里。README、门户同一说法
- **资产体检**：资金规整方案里金额是 0 的那一份不再列出来
- **Web 版演示数据**：网址带 `?demo=1`（只在账本为空时）写入一套普通上班族的三个月数据，`?theme=night` 这类参数直接切主题——门户截图用，手机上不会走到

## 0.9.13 — 2026-09-25

- **首页头卡**：大字仍是「可花的」，下面一行改成 现金余额 / 总资产 / 本月支出 / 本月收入（「余额」这个叫法太含糊：现金余额 = 现金、银行卡、钱包、锁仓相加；总资产 = 所有账户里正的钱，含投资，不扣负债）。游戏层关着时的大字也改叫「现金余额」
- **暖心话轮播**：头卡底下那句寄语从一句变成一池话轮着来（每 7 秒淡入淡出换一句，点一下立刻换，每天从不同的一句开始，切到别的页不转）。三档：负债重 / 收入低的是打气的话（二十多句），中间的是定心的话，收入高、存款够花半年以上的是有点情怀的句子（「行到水穷处，坐看云起时」「真正的富足，是可以对不喜欢的事说不」……）
- **花呗 / 微信分付 / 京东白条 / 抖音月付**：负债页「添加信用卡 / 花呗」里先选是哪一类，账单日 / 还款日 / 计息方式 / 违约金按各家公开规则预填（都能改，以自己 App 的账单页为准）：花呗 1 号出账 9 号还（可选 5→15、10→20），按时还清免息、逾期未还部分日万分之五；分付每月 1 号出账，从借的那天起按天计息（按时还也有利息），逾期利率 × 1.5，账单不到 100 元要全还；白条账单日后 9 天还，逾期每天 0.07% 违约金；抖音月付 1→6 / 10→15 / 15→20。计息方式从两种扩到四种（全额 / 未还部分 / 到期后才计息 / 按天计息），另加「逾期利率倍数」「每天违约金」
- **信用卡建好后能改名字了**：详情页右上「设置」里名字、额度、日子、利率一起改
- **还款计划**（更多 → 还款计划，负债页 / 财富页也有入口）：按发薪日、每月收入、各张卡 / 花呗 / 白条的账单日和还款日、贷款的每月还款日，从今天排到第二个发薪日前，每一笔写清哪天、还多少、还完手里剩多少。钱够就全额还；不够就先保住到下次发薪前所有月供和最低还款（不上征信），其余能还多少还多少，并写明缺口
- **资产体检 + 调优方案**（更多 → 资产体检）：净资产、现金余额、总资产、总负债、月收入、月支出、够花几个月一张表；逾期 / 发薪前缺口 / 应急金 / 月供占收入 / 额度用了几成 / 储蓄率 / 高息负债逐条给依据；调优方案按先后排（先还逾期 → 补缺口 → 高息先还 → 攒应急金）。不缺钱的给资金规整方案：应急金（有负债 3 个月、没有 6 个月）留在随时能取的地方、一年内要用的单独放、长期闲钱考虑低风险去处——余见不卖理财、不推荐具体产品。负债的人顶上多一句打气的话。全部按账本规则算，不调模型
- **日历标注还款日**：贷款月供、固定支出、信用卡 / 花呗 / 白条的还款日（本期写还剩多少、下期写约多少）、出账日、发薪日，格子右上角一个点（红 = 要还，绿 = 发薪，灰 = 出账），点那天在下面列出来
- **关于余见**：新页面——这是什么、开源免费无广告、账本不出手机、一个人在做，源码 / 官网 / 隐私声明 / 反馈 / 支持入口
- **反馈 BUG / 建议**（更多 → 关于）：文字 + 最多 4 张截图 + 可选联系方式；附带信息（版本 / 系统 / 屏幕尺寸 / 主题）逐条列在页面上、可以不带。发到 yujian.ivyea.com，存下后转到作者的飞书，截图不放任何公开链接；只在点「发送」时出网，记进出网记录；纯本地模式下不能发。隐私声明同步写明
- **支持余见**：加了一句「你的支持是我持续更新的动力，也是对这个项目的认可」
- **玻璃类主题（玻璃 / 素纸 / 清新 / 卡通 / 樱花 / 夜玻璃）卡片右侧一条灰、屏幕下方的卡片整片发灰**：只在部分手机上出现。卡片的玻璃是从一张预先模糊好的背景图上按屏幕位置取样的；这张图是半分辨率（比如 206×457），有的机型的图形后端会把它补齐到 2 的幂（256×512），着色器按整张图取样，读到的是补出来的空白——宽度约 80%、高度约 89% 以外就是那片灰。0.9.11 当成投影透出来的灰去修了，没对上。现在背景图先拉伸铺满 2 的幂尺寸再交给着色器，没有可补的边

## 0.9.12 — 2026-09-24

- **负债页只有信用卡欠款时「预计还清」写着「已还清」**：它只按贷款的每月还款推算，没有贷款就当成还清了。现在只有信用卡时写「按账单还」，真的一分不欠才是「已还清」

## 0.9.11 — 2026-09-24

- **「可花的」比「余额」还多**：两个数不是一个口径。首页「余额」把所有账户加在一起（信用卡、贷款的负数、投资都算进去）；「可花的」却从「正余额的现金类账户」算起——透支成负数的钱包（常见于自动记账记了支出、却没填期初余额）直接被忽略。于是有贷款 / 负数钱包的人，可花的就能比余额大。现在两边同一个起点：**余额 = 手头的钱**（现金 / 银行卡 / 钱包 / 目标锁仓直接相加，负的也减），可花的 = 余额 − 锁仓 − 发薪前的固定支出和还贷 − 信用卡待还，永远不超过余额。信用卡、贷款、投资在财富页的净资产里看；桌面小部件的余额同口径
- **信用卡：额度、账单、利息、违约金**：负债页可以「添加信用卡」（额度 / 现在欠多少 / 账单日 / 还款日），已有的信用卡点一下补上。按流水算：额度按此刻欠款（还进去马上回来、再刷再占），账单按账单日那天的欠款，账单日之后刷的进下一期。显示本期账单、已还、还剩、最低还款（默认 10%，超额度的部分全额计入）、到期日；到期前能看「只还最低 / 一分不还」要多付多少，逾期后算违约金（没还够的最低还款 × 5%）和利息（日万分之五，按每笔消费的记账日起算到下次出账，全额计息 / 未还部分计息可选）。利率、比例、违约金最低收费都能改。7 天内到期或已逾期的卡挂到首页「近期到期」；对话和 MCP `get_wealth`（新增 `credit_cards`、`cash_on_hand`）能答信用卡的账单
- **深色主题**：新增 夜玻璃（深色的玻璃）/ 墨夜（近黑实色，OLED 省电）/ 夜木（炭褐暖色）三套；外观页分浅色 / 深色两组。可选「跟随系统深色」：系统切深色时自动换成选好的深色主题，切回浅色换回原来那套（默认关，不设就和以前一样）。深色下状态栏图标变白、分类圆片压暗、投影改成黑影
- **玻璃主题的灰影**：玻璃卡片和底栏胶囊的投影以前连形状内部也铺了一层，玻璃面一透（底栏本来就半透明，卡片在着色器没加载 / 背景没截好时也会退成半透明），里面就透出灰——底栏的实时模糊还会把这层灰和滚过去的字糊在一起。投影改成只画在形状外面（玻璃 / 素纸 / 清新 / 卡通 / 樱花 / 夜玻璃）
- **收入在全国的位置 + 一句寄语**：首页头卡底下、财富页新增「你在哪儿」——按国家统计局 2025 年全国居民人均可支配收入五等份分组估算「收入超过全国约 X% 的人」（统计局不公布分年龄的分布，所以只和全国比，不编「同龄人」）；再配一句按处境挑的话：负债重 / 收入低的给劲，收入高、存款够 6 个月以上的给点意境，中间的给定心。一天一句不跳
- **「模型与语音」里「OpenAI 兼容 / Anthropic」比下面的输入框短一截**：分段按钮只按内容宽排，改成撑满卡片和输入框对齐；周期账单表单里的「支出 / 收入」同改

## 0.9.10 — 2026-09-21

- **周任务候选能改、能删了**：以前候选只有一个「＋」——模板给的数字（外卖不超过 3 次、餐饮不超过 ¥80）改不了，不想要的也划不掉，而且候选每次打开按上周账本重算，划掉了下次还冒出来。现在点一下候选 → 表单带着它的数字打开，改完加入本周（原候选一并拿掉）；左滑或长按「不要这个」→ 本周不再出现（换周重算）。模型提的候选和模板撞车的不再重复列。「自己写一个」多了「往某个目标存至少 N 元」一档（有锁仓目标才给选）
- **支持余见（¥1，自觉制）**：更多页最上面多一张卡——记满 30 笔或用满 14 天才出现，不是装上就催。点进去：支付宝一键拉起付款页 / 微信把收款码存进相册再拉起扫一扫；付没付都可以点「我已支持，永久关闭」，不校验；也可以「30 天后再说」。开着「支付页识别」或「通知自动记账」的，付完那笔 ¥1 会被自动认出来（点付款按钮后十分钟内、金额正好 ¥1 的支出），提醒自动关掉并在对话里道谢。「支持过」只是账本画像里的一个日期，随同步走，换机重装不再问；成就里多一枚「支持者」。整页不联网、不上报——见隐私声明。「关于」里永远有入口，不付也一样用

## 0.9.9 — 2026-09-21

- **首页余额到六位数折成两行**（¥-399937.25 被拆成「¥-399937.2」和「5」）：头卡下面三格原来各占三分之一，余额一长就比格子宽。现在三格按各自内容宽排开，整行放不下才等比缩小，金额永远一行、不截断；头卡的大字金额同样加了兜底；负债页的三格由「截成 ¥399…」改成缩小
- **对话页「手动记一笔」弹开 / 收回卡**：两处。一是弹层给键盘让位的那层每帧重建整张表单，收回时键盘和弹层一起动、表单一帧一帧重排——现在表单只造一次、退场时留位钉住，只剩一个滑出动画；二是底栏五个标签页一直都活着，键盘升降每一帧都让看不见的四页跟着重建（首页 / 记录页的 build 里是一串查库）+ 重排——看不见的页现在拿一份钉死的屏幕参数，只有当前页跟着键盘动。对话页自己的键盘升降同样受益
- **更多 → 审计日志 点开 / 返回没有转场动画**：300 条改动塞在一张卡的 Column 里，进页第一帧要把 300 个 ListTile 全建全排，那一帧长到把整段转场吃掉。改成和「记录」页同款的按天一卡（一天太多再切成几张），露出来的才建
- **首页「比平时高」一挂挂一个月**：原来按整月算，1 号一笔大额能提醒到月底。它是「刚发生的一笔看一眼」，改成只看最近 7 天；左滑划掉的按交易 id 记住不再出现（对话里问「这个月有没有异常支出」仍按整月答）
- **模型与语音里「高级：老账号用 App ID + Access Token」按下去一块灰色长方形**：折叠行的按压高亮是按整行宽画的矩形（全局把折叠区的形状设成无边框后，水波没有圆角可裁），夹在有内边距的表单里很突兀。表单里的折叠行统一换成不带按压高亮的（主模型页「可选：看图用另一个模型」、学模板弹层「高级：手改规则」、MiniMax 的「高级」同改）；展开动画本身就是反馈

## 0.9.8 — 2026-09-20

- **首页目标小卡旁边一块直角灰底**：三个目标起用的横滑条，视口只有卡片那么高、宽只到页边距，卡片投影（24 的模糊）被视口裁成一块贴着卡片的直角灰底。横滑条视口改成铺满屏宽（页边距在列表里自己留，卡片仍和上下的卡片对齐）且不裁，投影照常散开；顺带第三张卡的边真能露出来了（以前差 1 像素被挡在页边外）
- **目标页长按拖动是一块直角板子**：ReorderableListView 默认把拖起来的那项包成一块带阴影的 Material（直角，还连着卡片底下的间距）。换成透明代理 + 轻微放大，拖起来仍是那张圆角玻璃卡
- **自动记账页加一句实测经验**：支付页识别开着时提示「付完款在支付成功页停两秒再走，识别更稳」（页面要画完才读得到字，微信这种自绘页面还要截一帧本机认）
- **「模型与语音」里折叠区展开后上下两条横线**（云端转写 / 高级：老账号用 App ID…）：ExpansionTile 展开时默认画上下分隔线，横穿卡片。全局关掉（主模型页、学模板弹层里的折叠区同改）
- **目标详情右上的菜单**：原来是 Material 的直角浮板（和玻璃卡片不是一个调）。换成和选择器同款的底部动作单（标记达成 / 归档 / 删除，删除用警示色）
- **目标能删了**：目标详情右上菜单加「删除」（和「归档」并列：删了不进已归档）。锁仓里的钱先释放回来源账户；锁仓账户没记录的真删、存过钱的归档留历史（和删账户同一条规矩）；还清目标只删目标本身，负债去负债页删
- **净资产为负也有称号——负翁那套**：以前资产减负债是负数的人挂的还是「贫困户」（按够花几个月算的最底档）。现在净资产为负按欠的钱分档：小负翁（< 1 万）→ 负翁（1 万起）→ 大负翁（10 万起）→ 百万负翁 → 千万负翁；首页角标用警示色、依据写「欠多少」，财富页写「再还多少降一档 / 转正」+ 负翁阶梯，等级（够花几个月）照算。更多页副标题、桌面小部件、对话上下文、MCP `get_wealth`（新增 `debt_tier` / `to_lighter_debt_tier`）同一来源

## 0.9.7 — 2026-09-20

- **负债能删了**：负债页长按一笔 → 删除。没还过款的：账户、每月还款提醒（含停掉的旧条）、还清目标一起真删；还过款的：账户归档保留历史（账户页「已归档」能恢复），另外两件删掉，负债页不再显示。发薪账户 / 记忆里指着它的引用一并清掉；同步到别的设备：对方没它的记录就跟着删，有就退成归档
- **账户能删了**：账户页点一个账户 → 「删除」（只对没有任何交易记录的账户开放；有记录的只能归档，删了历史对不上，对话框里写明）。删负债账户走负债那套，连带清还款提醒和还清目标
- **预算卡片按下去是一块长方形**：按压高亮的内边距套反了（在 InkWell 外面），高亮只盖到卡片中间一块矩形。挪进去，撑满卡片、随卡片圆角裁掉；首页预算提醒同改

## 0.9.6 — 2026-09-20

- **主题互切还闪（灰一下）的真根因**：透明底色用的是 `Colors.transparent`——那是「透明的黑」，切主题插值到实色的中途是半透明深灰，整页灰一下，和底下有没有背景层无关。现在切主题不再插值（直接切），透明底色也改成「底色的 0 透明度」
- **4×2「本月」**：余额大字右边放「今日支出」，右上不再空一块

## 0.9.5 — 2026-09-20

- **修主题互切黑闪**：切主题时 ThemeData 会插值 200ms，实色主题的 Scaffold 底色从「透明」渐变到实色的那几帧是半透明的，底下没有背景层就露出窗口的黑（玻璃 / 暖木 → 留白 / 硬边 / 柔感，或反过来）。现在没有背景层的主题在 Navigator 底下垫一块主题底色
- **4×2「本月」小部件再排**：四段从上到下铺满——头行（余额 + 称号 | 月份）→ 余额大字 → 支出 / 收入 / **结余** 三格（撑开中段，随高度居中）→ 最近一笔 | 记一笔。不再上下空一截

## 0.9.4 — 2026-09-20

- **4×2「本月」小部件重排**：改成左右两栏——左：余额（+ 称号）大字、最近一笔；右：月份 / 支出 / 收入 靠右一列 + 记一笔。以前支出、收入、按钮挤在下面一行，右半边全空
- **2×1「余额」小部件**：称号胶囊只露一个字的根因是「＋ 记一笔」四个字的按钮把左边挤没了；按钮改成一个「＋」圆钮，称号胶囊独占一行
- **新增 2×2「目标（小）」小部件**：可花的 + 最多 2 个目标（名字一行、进度条、百分比 · 还差多少）

## 0.9.3 — 2026-09-20

- 最底档称号「穷逼」改为「贫困户」（直白但不骂人）
- **4×2 目标小部件不再留一大块白**：每行平分高度，字号加大；目标少于 3 个时行里多一行「已攒 / 目标」，少于 2 个时底部放「记一笔」；没目标时提示 + 按钮
- 补齐卡片：周任务（候选 / 过去几周）、财富（成就 / 设置）、模型（主模型表单 / 开关与用量）、语音（听你说 / 它说话）、审计日志、同步与云备份（服务器 / 加密备份）

## 0.9.2 — 2026-09-20

- **称号不用再等一个月**：以前等级 = 流动资产 ÷ 近 3 个整月的平均支出，新用户要记满一个月才有称号。现在「月支出」按一条估算链取：你手填的（财富页新增「每月大概花多少」）→ 历史整月均值 → 近一个月的收入（第一个月先按月光算，几笔支出外推出的「够花一百个月」不算数）→ 本月到今天按天外推（≥ 3 天）→ 周期账单和还贷合计。录完第一笔工资就有称号，财富页写明是按哪一条估的
- **今天还能花**：以前 = 可花的 ÷ 天数 − 今天已花，但可花的来自余额、已经扣过今天的支出，等于扣了两次——可花 150 却显示 0。现在 = 可花的 ÷ 到发薪日的天数，「今天已花」只做展示
- **负债**（更多 → 记账 → 负债）：房贷 / 车贷 / 网贷 / 借款一张表单——填「还剩多少、每月还多少、几号还、从哪个账户扣」，余见建好三件：负债账户（余额 = 还剩多少要还，本息合计不拆）+ 每月还款的周期转账（到期进收件箱，确认了余额就少一期）+ 还清目标（跟余额走，挂在首页目标条和小部件上）。负债页：总负债 / 每月还款（占本月收入几成）/ 预计还清，每一笔带已还进度；点一笔可设或改每月还款。财富页净资产改成「= 资产 − 负债」，有负债时多一张负债卡；MCP `get_wealth` 多返回 `assets` / `debt` / `card_owed` / `spend_basis`
- **可花的与等级把负债算进去**：可花的 = 流动资产 − 锁进目标 − 发薪前要付的固定支出**和还贷** − **信用卡待还**（刷了就扣，还卡时不再扣）；等级的月支出 = 生活支出 + 每月还贷（房贷每月真金白银出去了，生存月数不能装作没有）。进账 1 万、还完贷剩 2 千的人，首页看到的是正数 2 千，净资产为负只在财富页如实标出并配还清进度
- **首页**：可花的下面只留一行「今天还能花 ¥x · N 天后发薪」（三行公式挪去财富页）；目标卡 1 张通栏（多一行已攒 / 目标）、2 张平分、3 张起才横滑；预算 / 比平时高 / 近期到期三块进卡片，和「最近」一套
- **卡片统一**：记录页按天一组一张卡、账户 / 分类 / 周期账单 / 预算 一组一张卡、自动入账记录一张卡默认露 5 条（可展开）、人格列表与「它记住的事」（默认露 8 条）、数据页两组。模型 / 语音这类表单页不套卡（输入框本来就有底，再套是双层框）
- **提示不再被底栏挡住**：以前 SnackBar 贴屏幕底、藏在悬浮胶囊下面，磨砂把它的颜色透出来（「大胶囊变色」）。现在标签页里的提示抬到胶囊上方；Shell 外层多余的一层 Scaffold 去掉（它也画了一份提示在胶囊下面）
- **对话「识别截图」在纯本地模式 / 没配模型时走本机**：Android 上用 ML Kit 本机 OCR + 规则直接起草，图不出手机；别的平台才提示去配置
- **桌面小部件新增 4×2「目标」**：可花的 + 称号 + 最多 3 个目标的进度条（攒够 / 还清的变绿），点开直接进目标页；没目标时提示建一个
- **四套新主题（不是玻璃）**：留白（白纸黑字，只有发丝线）、硬边（奶白底、2px 墨线、右下硬影，新粗野主义）、柔感（卡片和底色同色，靠双向光影鼓起来）、暖木（米色纸、木色数字、暖影）。主题系统支持实色卡（`glass: false`：卡片 / 底栏不取样不模糊，描边和投影由主题自己定），玻璃五套不变

## 0.9.1 — 2026-09-20

- **财富称号**：六档等级各配一个称号，嘲讽但真实——够花不到半个月是「穷逼」，往上依次「月光族」（0.5–1 月）、「温饱户」（1–3 月）、「小康」（3–6 月）、「中产」（6–12 月）、「人上人」（≥ 12 月）。称号只是给「生存月数」起的名字，算法没变
- **首页**：头卡右上角的等级角标换成称号胶囊——称号加粗做主，「够花 N 个月」跟在后面做依据；点卡片进财富页。游戏层关着时和以前一样不显示
- **财富页**：等级卡大字改成称号、等级名跟在旁边；「再攒多少升到」也报称号；六档一览每个 chip 都带称号
- **桌面小部件**：4×2、2×2、2×1、4×4 都加了称号胶囊（1×1 只有一个按钮放不下）。2×2 头行放不下三样，月份并进「9 月支出 ¥…」那行。App 没推过数据 / 没称号时胶囊整个隐藏，不留空壳
- 人格的升级播报说的是称号（「主人现在是「温饱户」～」），等级名进小字；对话上下文里也带上称号；MCP `get_wealth` 多返回 `title`

## 0.9.0 — 2026-09-20

**财富游戏层：把真钱做成游戏币。** 游戏里人人会攒钱，是因为钱有目标、有进度、有等级、花出去看得见代价；这个版本把这四样贴到真实账本上。所有数字都是账本推导的真数，没有虚拟币、积分商店、签到。

- **目标**（更多 → 目标）：心愿（换手机 / 买车 / 首付 / 旅行，8 个模板带典型价位和拆项）、应急金（N 个月支出，自动算并冻结）、还清（跟着信用卡 / 应付账户余额走）、净资产里程碑。三步建：要什么 → 多少钱什么时候 → 怎么攒。存入规则四种：每月定存、工资到账比例、零头凑整（每周一结一笔，不淹账本）、周任务奖励。钱放哪两种：**虚拟锁仓**（默认，钱不动、只从「可花的」里划走，存入直接记）/ **真锁仓**（绑定余额宝等真实账户；余见不做银行直连，每次存入进收件箱，你真转了再确认）。存入 = 真实转账，兑现 = 从锁仓记支出（可多笔），完成 / 归档时剩余按存入来源比例释放；进度语言：「还差 4 200 · 按现在的速度 5 个月零 10 天 · 比计划晚 12 天」；列表拖动排序 = 发薪日分钱的优先级。对话里说「我想攒 5000 换手机」出一张「建这个目标」卡；问「日本游攒了多少」直答（都不出网）
- **首页**：第一眼看「可花的」= 流动资产 − 锁进目标 − 到发薪日前的固定支出（周期账单里已知的），第二行「到 X 号发薪，今天还能花 N」；等级角标；目标条与本周任务卡。余额降为下面一格（是真实余额，锁仓的钱也算在内）
- **财富页**（更多 → 财富）：等级（= 生存月数 = 流动资产 ÷ 近 3 个月平均月支出；起步 / 喘口气 / 站稳 / 安心 / 从容 / 自由感，写明升下一级差多少）、可花的、今天还能花、净资产（信用卡 / 应付为负）、本月储蓄率、收入线（主线 = 工资奖金 / 副本 = 兼职礼金 / 挂机 = 投资收益），每个数都写清算式；发薪日（没填就从收入记录推，推不出按月底，首页标明来源）与工资账户
- **周任务**（更多 → 周任务）：五种本机判定器——某分类本周 ≤ N、某商户关键词 ≤ N 次（「外卖」按美团 / 饿了么等关键词数，因为不是分类）、至少 N 个无消费日（周期账单生成的房租不算破功）、每天记账、往目标存至少 N；候选按上周账本从模板挑，有模型再提 2～3 个（只进候选，勾了才算）；也可以自己写；周日结算，延后到下次打开也算；做不到不扣任何东西；完成奖励 = 往目标里存真钱
- **成就**：22 个，全部由账本事实触发（目标 10/25/50/75/100%、兑现、应急金 1/3/6 个月、储蓄率 20/30/50%、任务首成 / 四周连胜、第一笔副本 / 挂机收入、负债清零、连续 30/100 天有记录、满 1000 笔），一次性，点开看依据
- **三个仪式**：发薪日——你确认了一笔工资 / 奖金收入（或到了发薪日）→ 按目标顺序分钱，一组转账草稿进收件箱，卡上写明剩下可花多少、每天多少、钱不够时谁少拿了；周一——结算上周（人格化播报）+ 本周候选；月末——进入新的一月第一次打开，对话里由人格讲上个月发生了什么、每个目标近了多远、下个月改一件事（有模型时用人格口吻重讲一遍，数字一个不改）。都在打开 App 时触发，**不发系统通知，不在后台跑**
- **收件箱代价行**：确认一笔支出时多一行「这笔 = 日本游晚 4 天 · 本周餐饮还剩 32」；只显示不拦
- **人格**：9 个新事件（建目标 / 存入 / 里程碑 / 达成 / 任务完成 / 未完成 / 升级 / 发薪 / 月复盘），六个内置人格各有话术，自定义角色自动生成，导入的旧人格包不用改（有通用回退）
- **开关分两层**：目标是理财功能不能关；「财富游戏」总开关（财富页）只管表达层——可花的 / 等级 / 代价行 / 仪式 / 成就提示，关掉首页恢复原样
- 隐私：新增出网只有两条（有模型时的周任务候选与复盘润色，只发账本速览和目标名），隐私声明 / 出网记录已同步；纯本地模式下不发生
- 底层：Ledger Core 新增 goals / tasks / achievements / profile 四张表（schema v5），锁仓是新的账户类型 vault（不出现在账户页和模型上下文，算进流动资产和净资产）；全部进同步变更日志、导出 / 导入 / 备份恢复、审计（大白话）；MCP 新增 list_goals / get_wealth / list_tasks / propose_goal / propose_deposit；未知枚举值不再让读库崩溃

## 0.8.19 — 2026-09-20

- 许可证由 MIT 改为 **AGPL-3.0**（LICENSE 全文替换；README、隐私声明、隐私页、门户页脚同步）

## 0.8.18 — 2026-09-20

- **出网记录**（更多 → 隐私 → 出网记录；用量与花费页也有「逐条记录」标签）：软件每一次把数据发出手机都记一行——什么时间、哪个模型 / 哪个域名、多少 token、发了多少字 / 多大的图或录音、成没成、耗时——点开有一句大白话说清发了什么、发给谁、为什么（「把你说的这句话（已打码）连同账户名、分类名、最近 10 笔记录发给 api.deepseek.com，让模型理解你要记哪一笔」）。所有模型调用经同一层记账，失败的也记（数据已经发出去了）；云端语音合成 / 转写、同步与云备份、拉模型列表、测试连接、版本检查、安装包与离线语音包下载全部覆盖。没有记录 = 没有发生过通信
- **纯本地模式**（更多 → 隐私）一键开关：禁掉所有可能把数据发出手机的功能——模型（连内网端点也不调）、云端语音合成 / 转写、截图记账的「发文字 / 发原图」档、同步与云备份、每天一次的自动版本检查；只用本机识别（通知模板 / 无障碍 / 本机 OCR / 离线语音包）和本机规则。配置全部保留，关掉就恢复；手动点「检查更新」和下载离线语音包仍然可用（只下载不上传）。老用户默认关，行为不变
- **隐私声明**（更多 → 隐私）：按代码里实际存在的每一条出网路径写——哪些数据会出手机（发什么 / 发给谁 / 什么时候 / 怎么关）、哪些永远不出手机、手机系统自带的朗读 / 识别引擎不受余见控制、每个权限要来干什么、承诺（无后门、无统计 / 广告 SDK、不运营任何接收数据的服务器、源码开源可查）、以及你怎么核对（出网记录 / 断网照常用 / 抓包）
- **自动记账设置教程**（自动记账页顶部 / 隐私页）：三条路各要什么权限、去哪开、怎么开（含小米 / HyperOS、华为、OPPO、vivo 的路径和「受限设置」）、国产系统后台保活四步、用不着的权限；每个系统危险权限警告之前先说清楚它是 Android 的统一措辞、余见拿到后只做什么、内容不出手机。自动记账页拨开「支付页识别 / 通知自动记账」跳系统设置前也会先弹这段话
- 审计日志改成大白话（「你确认了收件箱里的一笔，已入账：¥28.00 · 午饭」「自动记账提了一笔草稿进收件箱」），标明来源（本机规则 / 模型 / 通知模板 / 截图本机 OCR），并说明它只管账本、通信在出网记录里
- 用量与花费：「统计 / 逐条记录」两个标签；测试连接也计入出网记录

## 0.8.17 — 2026-09-19

- **修：翻微信聊天记录 / 账单，收件箱多出一堆**。支付页识别原来只看整页有没有「支付成功」四个字、再取全页第一个像金额的数——聊天里的支付凭证卡片、账单列表、账单详情页全中招；截屏那条路每切一次窗口就截一帧，滚一屏换一批金额。现在先按页面结构判「是不是刚付完款的那一页」：一屏里多个独立「支付成功」（多张凭证卡 / 列表）、「支付成功」不在上半区、上半区金额太多、页面上有 10 分钟以前的时间（历史账单详情）、有聊天页标志——任一条都整页拒掉，诊断里写明原因。金额取上半区里字最高的那一行（成功页的大字），不再取「第一个」；下半区的推荐商品价不算数
- **修：真正付款反而没识别到**。付款是「密码弹窗 → 成功页」两次窗口切换，成功页那一帧落在截屏限速内被直接丢掉、连日志都没有；现在限速内的请求延后到期再截，不丢。另外窗口切换后 8 秒内的内容变化也允许截屏（成功页若不是新窗口、只是同一页换内容，以前一帧都截不到）
- **修：截图记账把支出认成进账**。方向原来是整页找「已收款 / 退回 / 到账 / 转入」这类词、见词就判收入——转账给别人的页面也写「已收款」，付款页脚注「1 天内未收款将退回」，银行页「入账账户」。现在分级取证：金额自带的正负号（账单上的 -25.00 / +25.00）→ 页面标题（支付成功 / 退款成功 / 转账成功）→ 金额所在标签（退款金额 / 实付）→ 对方标识（转账给 / 来自）→ 才轮到明确的收入词。「优惠金额」不再被当成付款金额
- 支付页识别：同一 App 同一金额 10 分钟内只起草一笔（按「完成」回到聊天页时那张凭证还在屏上）；11 分钟后同金额算新的一笔
- 通知自动记账：微信里朋友发的消息（标题是人名）正文里提到「微信支付 / 已支付 / 转账」不再被当成支付通知；聊天软件的通知没有 ¥ 符号不走兜底模板。微信合并通知「[2条]微信支付: …」照常认；短信 / 银行 App 不受影响
- 识别诊断多两类记录：「有支付成功但不像刚付完款的页面（原因）」「截屏限速，稍后补截」；已入队和拒掉的记录带页面类名，攒真实类名给下一步做正向过滤

## 0.8.16 — 2026-09-19

- **支付页识别：微信支付页读不到字 → 截屏 + 本机 OCR**。诊断里一直是「有窗口但一段文字都读不到」，声明成「无障碍工具」也没用：微信的支付页是自绘界面，节点树里本来就没有文字，不是屏蔽的问题。现在无障碍服务在节点树读不到字时截一帧屏幕、在本机 OCR 出文字，再走同一套规则；位图只在内存里过一下，不落盘不上传。需要到系统无障碍页把「余见 · 支付页识别」关一下再开，让系统授予截屏能力
- 识别诊断的几条判词改成实话，截屏这条路的每一步也记进去

## 0.8.15 — 2026-09-19

- **修：键盘收起时对话页输入框先掉到底栏下面再跳回来**——0.8.13 的留位是个开关（键盘在就不留），键盘收起的动画里输入框一路跟着键盘掉到屏幕底、掉到底再一下跳到胶囊上面。现在留位随键盘连续变化：页面底边始终 = max(键盘顶, 胶囊顶)。胶囊自己的下边距也改按 viewPadding 算，键盘动的时候不再跳

## 0.8.14 — 2026-09-19

- **截图自动记账默认改为「仅本机」，图不再上传**：本机 OCR（ML Kit 中文模型打进 APK，离线、不依赖 Google 服务）把图上的字读出来，交给本地规则认金额 / 方向 / 商户 / 时间 / 账户，整个过程图和字都不出手机。以前是每张新截图原图发给视觉模型——包括聊天、照片这些和钱无关的图
- **三档可选**（自动记账页 → 截图怎么认）：仅本机；本机认不出时发文字（图不出手机，OCR 文字先打码卡号 / 手机号 / 订单号再发给你的文本模型）；发原图（老做法，明写了隐私代价）。三档都先在本机判「像不像一笔交易」，聊天 / 照片 / 网页截图在这一步就丢掉，任何一档都不会把它们发出去
- 升级上来的老用户：截图记账从「发原图」变成「仅本机」，排版乱的小票可能认不出——处理记录里会写明「本机没认出」，可自己切档
- 对话页里主动发的图不变：原图直接发给模型
- 自动记账页的说明改成实话：三条路默认都只在本机处理

## 0.8.13 — 2026-09-19

- **修：键盘弹出时对话页输入框被顶到屏幕上半截**——0.8.11 给首页各页注入底栏留位时，把带着键盘高度的外层 MediaQuery 塞回了 Scaffold 体内，里面的页面又让了一次键盘高度。现在只改体内的那份，键盘弹出时也不再额外留胶囊的位

## 0.8.12 — 2026-09-19

- **修：0.8.11 底栏胶囊变高**——NavigationBar 自带四边 SafeArea，以前在 Scaffold 底栏槽里由 Scaffold 去掉顶部安全区，挂到 Navigator 外面后没人去，状态栏那几十像素被塞进了胶囊。现在四边安全区都去掉，只由胶囊外层留底部安全区

## 0.8.11 — 2026-09-19

- **修：切页瞬间底栏胶囊后面的字是清晰的（还是闪）**——底栏原来是首页 Scaffold 的一部分，切页时整条路由被套进淡入淡出层，Impeller 在那层里不给 BackdropFilter 做回读，那几百毫秒胶囊就没有磨砂。现在底栏挂到 Navigator 外面的覆盖层，永远在静止的合成层里，磨砂全程有效；推入 / 弹出子页、弹底部弹层时它跟着那条路由的动画滑下 / 滑回，和页面同步

## 0.8.10 — 2026-09-19

- **修：底栏胶囊里的黑线 / 返回时黑闪（0.8.9 没修干净）**——根因是把玻璃着色器当 BackdropFilter 用：引擎交给它的纹理不是胶囊本身（外扩、裁边、降采样、切页时整屏），坐标猜错就是黑线黑闪，而猜没猜对只有装到手机上才知道。现在底栏的 BackdropFilter 只做「模糊 + 饱和」（两个后端都有、没有任何几何假设），玻璃面（着色 / 亮边 / 暗线 / 面光）用普通着色器盖在上面，坐标确定，Skia 设备也是同一套样子

## 0.8.9 — 2026-09-19

- **修：底栏胶囊两端的白弧 + 黑弧，以及从子页面返回时闪一下**——同一个根因：底栏着色器拿到的「背景纹理」不是胶囊本身，而是按模糊半径向四周外扩、再被屏幕裁过的一块（切页时更是整屏），0.8.8 把它当胶囊来画，亮边 / 暗线画进了胶囊里、切页时又跑到别处。现在用胶囊的屏幕位置反推纹理在哪，形状定位准了；底栏同时调得更玻璃：着色更淡、折射带更宽、模糊更重
- **选项弹窗重做**：所有「分类 / 账户 / 类型 / 币种 / 周期 / 频率 / 音色」这类选择字段不再用 Material 那块直角白菜单——字段外观和输入框一致，点开是底部弹层（在对话框里是居中小面板）：标题 + 一列选项，选中项打勾着色、打开自动停在它附近
- **修：「手动记一笔」弹出 / 收回略卡**——以前弹层一出来就叫键盘，弹层动画和键盘动画叠在一起，键盘每升一帧表单整个重建、还重查一次分类和账户。现在弹层滑到位再叫键盘，分类 / 账户查一次缓存

## 0.8.8 — 2026-09-19

- **滚动 / 切页卡顿根治**：以前每张卡片都是 BackdropFilter，更多页一屏 7~9 次「回读底下画面 + 高斯模糊」，每帧都重来；切页时两页的模糊同时算还各套一层透明度，中端机直接掉帧。现在全 App 只剩底栏一块真 BackdropFilter，卡片改成从**预模糊的全局背景**按屏幕位置取样（背景是静止的，看不出差别，每帧只是一次 drawRect）；玻璃主题的全屏 sigma 60 模糊改成径向渐变色斑
- **UI 线程不再在 build 里跑上千条 SQL**：更多页「N 笔记录」以前把全部交易实例化再数（每笔还单独查一次 postings），且 AppState 任何一次 notify 都触发——现在是一句 COUNT；`listTransactions` 的 postings 成批取，消灭 N+1；日历页按天查而不是取最近 500 笔再筛（顺带修了老账看不到的问题）
- **自定义背景图**：按屏幕物理宽度解码（原来几千像素的原图整张进显存），可见度走画笔 alpha 不再包全屏 Opacity
- **玻璃质感重做（对标 iOS 26 Liquid Glass）**：着色降到 22%~40%（不再是 60% 的白纸），模糊后拉饱和度，边缘折射（背景在边缘被透镜弯一下）、左上受光的镜面亮边 + 背光暗边，背景暗时自动加重着色保证字能读；底栏在 Impeller 上是真折射玻璃，滚过的内容在边缘弯
- 更多 → 关于 → 长按「检查更新」：显示 / 隐藏性能层（每帧耗时条），卡顿时截个图就能定位

## 0.8.7 — 2026-09-19

- **修：微信「支付成功」页不自动记账**——诊断里一直是「有窗口但 0 段文字」。根因：Android 14 起，视图设了 `filterTouchesWhenObscured`（支付 App 防劫持标配）就自动算 `accessibilityDataSensitive`，不是「无障碍工具」的服务拿到的节点树是空的。服务现在声明为 `isAccessibilityTool`。**升级后要到系统无障碍页把「余见 · 支付页识别」关一下再开**，系统才会刷新声明；诊断卡会提示这一步
- **修：自定义背景切页闪一下主题色**——Flutter 的 FadeForwards 过渡在切换期间给被盖住的那页垫一层 surface 色，首页没被背景包着就露出来了；已关掉
- **真玻璃卡片，所有主题**：卡片改成半透明 + 背后真实模糊 + 顶部高光 + 发丝描边（iOS 那种），素纸 / 清新 / 卡通 / 樱花不再是死白色块，自定义背景图能透出来；底栏胶囊所有主题都磨砂
- **模型配置一个入口**：更多 → 「模型与语音」，两个标签页（主模型 / 语音）；申请 API、开通配音的教程、用量都从里面进，更多页不再平铺六项
- **删掉离线真人感语音包**（Kokoro，效果差）：不再提供下载；之前选了它的自动退回系统朗读，装过的在语音页有「删除」回收 145 MB
- 自动记账页「识别诊断」改成和「截图处理记录」一样的卡片，每条带图标；新增「有窗口但读不到文字」这一类，对应上面的根因
- 对话页按住说话：去掉输入区上方多出来的那条状态条，「松开发送 / 上滑取消 / 识别片段」都在按钮上

## 0.8.6 — 2026-09-18

- **一个模型就够**：「模型与 API」改成一个主模型（读字、看图、陪聊都用它），看图另配收进「可选」；教程里三家各一个按钮一次填全——硅基流动一把 key 连语音识别 / 合成一起配好；新增**阿里云百炼 Qwen-Omni**：一个多模态模型文字 + 看图 + 开口说话，语音页多了「主模型自带语音」选项（4 个音色）
- **接入豆包语音（火山引擎）和 MiniMax**：更多 → 语音里多了两个选项，短剧配音同款的云端大模型，有情绪有语调。豆包只要一个 API Key（老账号点「高级」填 App ID + Access Token），MiniMax 只要一个 API Key；音色下拉选，「语气」写一句话（"用撒娇甜蜜的语气"）豆包照着念、MiniMax 折成它认的情绪
- **云端朗读逐句流水线**：第一句合成完就开口，后面的边合成边播，长回复不用等整段（豆包 / MiniMax / OpenAI 兼容都一样）
- **「怎么开通真人感配音」教程**：更多里单独一页，两家从注册、开通、拿密钥到回余见填入，每步一个动作；常见报错的原因也写了
- 用量页把两家的合成字数也记进去（单价点行自填）

## 0.8.5 — 2026-09-18

- **修：朗读没声音**——开了离线语音包 / 云端合成后，合成"成功"却不播（打断计数在停止旧播放时把自己作废了）。对话页朗读和语音页试听都受影响
- **语音页改成三选一**：手机系统朗读 / 离线真人感语音包 / 云端合成，选哪个用哪个，一眼看到当前用的是哪个；云端的填空只在选了云端时才出现，云端转写收进「高级」
- **自定义全局背景**：外观 → 背景，从相册选一张当全屏背景，可见度滑块可调，所有主题都能用；可换、可移除
- **桌面小部件页**：预览按真实格子比例画（1×1 不再放成一大块）；「添加到桌面」点后有明确反馈，小米 / HyperOS 没弹确认框时告诉你去应用信息页允许「桌面快捷方式」；桌面真加上了会弹提示
- 更多页最后一项不再被底栏挡住
- 自动记账页的识别诊断 / 截图处理记录默认只展开最近几条，点「展开全部」再看；原生侧本来就只保留最近 40 / 20 条，不清空也不会无限长
- 底栏胶囊再宽一点、贴底一点

## 0.8.4 — 2026-09-18

- **截图自动记账**：更多 → 自动记账 → 「截图自动记账」。打开后相册里新出现的截图会发给你配置的看图模型判断：是支付页 / 订单 / 账单 / 小票就按当前模式记账（确认模式进收件箱，智能 / 静默直接入账），聊天记录、商品页、广告等一律忽略。没有「支付成功」字样的消费（订单页、账单）截个图就能记。App 没开着时靠通知监听 / 无障碍留着的进程在后台起无头引擎处理；进程被杀期间的截图，下次打开补扫最近 24 小时。页面里有每张图的处理记录（记了 / 进收件箱 / 忽略 / 出错）。需要相册读取权限
- **教它认一种通知**：自定义模板不再让人填包名和正则——从最近收到的通知里选一条（或粘贴文字），点一下哪个数字是金额、选个方向，规则自动生成，保存前就能看到「会记成：支出 ¥36.50 · 肯德基」。高级区才露出包名 / 正则
- **怎么申请 API**：更多 → 「怎么申请 API」，面向不知道模型去哪买的用户：三个概念、DeepSeek 和硅基流动两家逐步截图级说明、要填的值可复制、看完一键填进「模型与 API」
- **用量与花费**：更多 → 「用量与花费」，本月 / 累计 token（输入 / 输出）、云端语音合成字符数、按模型分列，按厂商标价估算金额（内置 DeepSeek / 硅基流动 / OpenAI 常见模型单价，不认识的模型点一下自己填）；所有对话 / 看图调用自动计量
- **更多页重排**：按「记账 / 自动化 / AI 与语音 / 外观 / 数据 / 关于」分组成卡片；原来塞在「模型与人格」一页里的东西拆成四页——模型与 API、语音（识别包 / 语音包 / 云端转写 / 云端合成）、人格与角色（选了就生效）、外观
- **离线真人感语音包**：模型与人格 → 「离线真人感语音包」下载（Kokoro v1.1 中文，约 145 MB，和离线识别包一样按需下载）。装上后它说话有语气有语调，在本机合成、不联网、不花 API 钱；8 种音色可选、可试听。优先级：云端语音合成模型 → 离线语音包 → 系统朗读
- **真人感语音**：模型与人格 → 「语音合成模型」填 gpt-4o-mini-tts / tts-1 / CosyVoice 这类，回复发到同一端点的 /audio/speech 合成后播放，有语气有语调；可填音色和语气说明，有「试听」。留空仍用手机系统朗读；云端失败自动退回系统朗读
- **桌面小部件**：更多 → 桌面小部件，五种小部件带预览图，支持的桌面上一键「添加到桌面」
- 对话页「识别截图」挪到快捷栏第一位，不用横滑；没配模型点它直接指路
- 首页去掉「说一句话记一笔」按钮（底栏正中就是对话）
- 底栏高度跟系统字号走：大字号手机上选中胶囊不再顶出底栏
- 修：0.8.3 发版时 `version.dart` 漏改，装上的 App 自认 0.8.2、每次启动弹「有新版本 0.8.3」。CI 现在校验 pubspec / version.dart / tag 三处一致

## 0.8.3 — 2026-09-18

- **底栏改成悬浮胶囊**：不再是贴着屏幕底边的一整条（那条硬分界线看着生硬），改成四边留空的圆角长胶囊浮在页面上，列表从它下面滑过；玻璃主题是真磨砂，其他主题沿用各自卡片的底色和描边。首页 / 收件箱 / 记录的列表底部相应留位，对话输入框仍在胶囊上方

## 0.8.2 — 2026-09-18

- **自定义角色**：模型与人格 → 「新建角色」，填名字、性别、年龄、身份设定、性格标签（可自己加）、怎么称呼你、说话习惯、口头禅、表情和主题色，保存即用；可建多个、可再编辑、可删。这些设定只进提示词的风格段，金额 / 确认流程它照样碰不到；没配模型时的回退话术也按称呼和口头禅生成
- **自定义头像**：人格列表里点头像可从相册选图（内置的猫娘、账房先生也能换），圆形裁切，只存本机；新建角色页里也能直接选。对话里的头像整体调大（气泡旁 30→42，顶栏 36，空状态 96）
- **日历上色**：日历页和 4×4 小部件的格子按当天净值上色——花得多粉红、进得多浅绿、今天蓝框，没账的天留白；金额加粗
- 导入的 JSON 人格包改为追加到列表而不是替换（旧的单个自定义人格自动并入）

## 0.8.1 — 2026-09-18

- **2×2 小部件修复**：添加后显示「载入窗口小部件时出现问题」——布局里用了裸 `<View>` 当占位，RemoteViews 不允许膨胀它。已换成带权重的布局
- **新增 4×4 日历小部件**：月历格子里写每天的支出 / 收入（今天高亮），顶部本月支出 / 收入 + 记一笔；点格子进记录页。跨月后 App 没打开过时只画空格子，不拿上个月的数字冒充
- **支付页识别更稳、能自查**：事件到了不再「限频直接丢」而是延后合并再扫（支付成功页刚出现时节点还没铺满，之前很容易错过）；遍历该 App 的所有窗口而不只活动窗口（MIUI / HyperOS 切窗期间活动窗口常为空）；读「对无障碍不重要」的节点（微信 / 支付宝金额常标成这样）；每次启动把原生侧开关和 App 设置对齐。「自动记账」页新增识别诊断：系统是否已开 / 服务是否绑着 / 最近事件 / 每一步日志（没到成功页、有字样但没读到金额、已送进收件箱……），没识别到时能看出卡在哪一环
- 支付宝金额写成 `-13.80` 的页面也能读

## 0.8.0 — 2026-09-18

- **语音输入修复**：0.6.0 起装了离线语音包后「按住说话」只闪一下「准备中…」就消失、松开也不发——录音其实开始了，但状态没进「录音中」，UI 以为没开始。已修，并加了回归测试；手指在引擎起来之前就松开（首次权限弹窗、慢机器）也会正确收尾，不再悬着一段录音
- **陪聊 + 记忆**：说的话不是记账也不是查账时，人格用你配的模型接着聊（能闲聊、回应情绪、结合今天 / 本月的真实数字），回复下面标着「模型名 · 陪聊」，一眼看出模型在用。它会记住你主动说过的关于自己的事（称呼、习惯、家人宠物、目标），带进之后的对话；「模型与人格 → 它记住的事」可查可删。隔半天再打开对话它会先开口问候。每条回复可点小喇叭单独朗读。新增古风人格「账房先生」；猫娘 / 陪伴者 / 账房先生会带小动作（如「（捻须）」）。没配模型时直说去配，不再是「没听懂」
- **自动记账两条路**：新增「支付页识别」（无障碍服务，可选）——微信 / 支付宝付款时 App 在前台不弹通知，通知监听抓不到，这条路在「支付成功」页面出现的那一刻读金额和商户，走同一套模板 / 收件箱 / 模式；只看支付 / 购物 App，只在支付成功那一刻读一次，内容不出本机。通知模板补了淘宝 / 天猫 / 京东 / 拼多多 / 美团 / 饿了么 / 抖音 / 滴滴 / 12306 / 携程等购物出行平台（支付 / 退款），物流 / 骑手类通知直接忽略。首页会提示还没开自动记账
- **桌面小部件**：添加面板里终于有预览图（之前只显示 App 图标）；新增 2×2「今日支出 + 本月 + 记一笔」；四个尺寸：4×2 本月、2×2 今日、2×1 余额、1×1 记一笔

## 0.7.1 — 2026-09-18

- 对话里发的图片显示缩略图（历史里也能看），不再是一串文件名
- 按住说话：松开一律发送，只有手指上滑到取消区才取消；被系统打断不再当取消；状态条浮在消息区上，不再挤动输入栏（之前布局一跳就把"松开"判成"取消"）；长按麦克风图标的诊断面板加了带时间的日志和「复制日志」
- 桌面小部件：冷启动时点「记一笔」也直接进对话（之前只有 App 在后台时才对）；新增 2×1（余额 + 记一笔）和 1×1（记一笔按钮）两种尺寸
- 日历视图：每天的支出 / 收入写在格子里，点一天看明细，月收支结余在下面；记录页、更多、对话快捷条都能进

## 0.7.0 — 2026-09-17

- 对话：微信式「按住 说话」（松开发送、上滑取消），左侧按钮切键盘 / 语音；助手消息带头像和气泡；人格会甩表情包；输入栏上方快捷条（手动记一笔 / 本月统计 / 预算 / 识别截图 / 朗读开关）
- 朗读回复：可开关，用系统 TTS
- 分类图标：内置分类自带 emoji 图标，记录、草稿、分类页、统计都显示；分类可换图标（一屏常用 emoji 或自己输）
- 手动记一笔：记录页右上角「＋」和对话快捷条，分类用图标格子点选
- 月度统计重做：收支总览（含日均）、每日趋势柱状图、分类占比环图 + 排行

## 0.6.0 — 2026-09-17

- **离线语音包**：sherpa-onnx + Paraformer 中文小模型（约 78 MB，从门户下载一次），装了就优先走，说话在本机识别，完全不依赖手机系统语音服务（小米 / HyperOS 上系统识别秒退、语音弹窗报错的都能用）。语音失败提示里直接给「下载离线包」；设置 → 离线语音包 可装可删
- 语音弹窗没返回结果也判为不可用；识别过程报错显示原因
- 玻璃主题：顶栏与背景一体不再有硬边；底栏更轻的磨砂；推入 / 返回页面时每个页面自带背景，不再两页透叠
- 自动记账：认不出金额的通知（验证码、聊天消息）不再进收件箱；猜不出分类的落到「其他」，静默模式照样入账，智能模式仍要求猜中才自动

## 0.5.1 — 2026-09-17

- 语音：系统识别「一开始就被结束、一个字没听到」也判为不可用并自动换下一条路（小米等机型的表现）；三条路都不通时每次都有提示并给「去配置」；长按麦克风看诊断（每条路各自的结论）
- 桌面小部件：内容垂直居中、余额更大、多一行「最近一笔」，不再留一大块空白

## 0.5.0 — 2026-09-17

- 语音输入三路自动降级：系统识别 → 系统语音弹窗 → App 内录音发到你的模型端点转写（设置里填「语音转写模型」）；哪条都不通时说清原因
- 五套主题：玻璃（默认，iOS 式磨砂）、素纸、清新、卡通、樱花；强调色仍跟人格走；首页余额独立颜色醒目显示
- 底栏「对话」移到正中；对话页名字可点击改；对话历史落盘，退出再进还在；可清空
- 分类认不出时落到「其他」照样能记，混合模式仍会请模型再分；草稿编辑里可就地新建分类
- 桌面小部件（余额 / 本月支出 / 收入 + 记一笔）；长按图标快捷方式（记一笔、收件箱）
- 应用内更新：启动每天查一次门户 version.json，Android 直接下载安装；「更多 → 检查更新」
- 门户 yujian.ivyea.com 上线，安装包由发版脚本自动同步

## 0.4.1 — 2026-09-17

- 新 App 图标（徽记，自适应图标 + Web + Windows）
- 模型名旁可从端点拉模型列表点选；厂商报错只显示正文一句话（不再整段 JSON）；模型名不对时指路
- 麦克风：权限没开只提示一次并给「打开应用设置」；先查权限再开始听
- 自动记账：系统拒绝「未知来源应用」敏感权限时给出「允许受限设置」指引和应用信息页入口

## 0.4.0 — 2026-09-17

- 交易编辑表单（金额/时间/账户/分类/商户/说明）、分类管理（子分类/改名/移动/删除）、账户编辑/归档/多币种
- 查询 forecast（月底预计/日均/还能花多少）与存钱算术；异常支出识别（首页 + 对话 + MCP）
- 系统分享进余见：文字进对话，图片进截图识别
- App 内麦克风语音输入（Android/iOS/macOS/Web），说完自动发出，草稿仍需确认
- Anthropic 原生 Provider；能力探测加看图；仅本地模型 / 发送前脱敏开关
- 导入：账单表格支持手工列映射；导出：Markdown 月报、SQLite 数据库文件备份与恢复（原生端，恢复会重置同步身份）
- 服务端 AI 代理（密钥放服务端，多设备共用）；语料补到 100 条
- 人格头像与主题色：内置五个人格各有 emoji 和强调色，自定义人格包可配 `emoji` / `accent`，App 强调色随人格切换

## 0.3.0 — 2026-09-15

- 可选 Yujian Server（server/，Python，Docker Compose）：同步中继 + 加密备份，不含账本规则
- 变更日志 + 多设备同步：推/拉/逐条应用，实体级 LWW，被覆盖方记审计；应用远端变更不回推
- 加密云备份：口令 PBKDF2 → AES-256-GCM，服务端只见密文；从服务器恢复
- 模型调用改用 package:http，Web 端也能接模型
- App 图标、apk 按 ABI 拆包
- 用户通知模板（App 内新建）、自定义人格包导入；自托管/人格/模板文档

## 0.2.0 — 2026-09-15

- 周期账单：到期只生成草稿进收件箱；补期上限 3；月末对齐
- 预算：分类（含子类）或总支出；退款按净额；按起始日对齐周期；提醒线；首页提示
- Android 通知自动记账：只读通知监听 + 内置模板（微信/支付宝/银行/泛用）+ 用户模板；确认/智能/静默三种模式；自动入账记录可撤销；模板试验框
- 截图/小票识别：视觉模型走同一套 JSON 归一与账本校验，置信打折进收件箱；可单独指定看图模型
- MCP Server：stdio JSON-RPC，只读 + propose 工具；Release 提供 Linux/Windows 原生 bundle
- 桌面：Linux / Windows 包
- 对话直接回答「固定账单有哪些」「预算还剩多少」

## 0.1.0 — 2026-09-15

首个版本。单设备本地账本，不需要服务器。

- 对话记账：规则解析优先（<10ms），可接任意 OpenAI 兼容模型补齐；金额与文本交叉核对；模型不可用自动退回规则
- 收件箱：一切候选先进收件箱，逐条可改、整组确认（原子），缺字段不能确认
- 账本核心：交易 + posting 轻复式，余额由流水推导；整数最小货币单位；退款不超原单；作废带原因；审计日志全链可追
- 查询：Query DSL，模型只出 JSON，引擎在账本上执行并给依据交易数；支持分组、对比期、余额
- 财务记忆：商户/关键词 → 分类/账户，收件箱里的纠正权重最高
- 人格：5 个内置人格；提示词五段分层，人格包只能改风格段
- 模型配置中心：OpenAI 兼容端点，测试连接真发请求测能力；API Key 进系统安全存储
- 数据：导出 CSV、JSON 全量备份、恢复（整库替换）、导入微信/支付宝/自家 CSV（去重、进收件箱）
- 平台：Android（签名 apk）、Web（IndexedDB 存储，用于演示）

已知限制：单币种统计不折算；无周期账单、预算、通知自动记账（0.2 计划）；iOS 未构建。
